class RequestModel {
  String from;
  String to;
  int id;
  int weight;
  String description;
  String image;

  RequestModel(
      this.from, this.to, this.id, this.weight, this.description, this.image);
}
